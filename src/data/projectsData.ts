import { Project } from "../types/project";

/**
 * Initial seed project list.
 * Set to an empty array so new users start with an empty Projects workspace.
 */
export const INITIAL_PROJECTS: Project[] = [];
