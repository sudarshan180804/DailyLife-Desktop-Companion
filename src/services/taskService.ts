/**
 * Legacy taskService re-exporting the modular TaskService.
 * Obsolete mock state has been migrated to src/modules/tasks.
 */
export { taskServiceModule as taskService } from "../modules/tasks";
